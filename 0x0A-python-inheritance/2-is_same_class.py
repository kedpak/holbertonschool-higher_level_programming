#!/usr/bin/python3
'''module: 2-is_same_class
'''


def is_same_class(obj, a_class):
    '''method: is_same_class
    '''
    if type(obj) is a_class:
        return (True)
    else:
        return (False)
