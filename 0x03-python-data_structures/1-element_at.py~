#!/usr/bin/python3
def element_at(my_list, idx):
    if idx > len(my_list):
        return (None)
    j = 0
    for i in my_list:
        if j == my_list[idx]:
            return my_list[idx]
        j = j + 1
