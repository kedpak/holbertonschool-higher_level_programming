-- script creates table called first_table in current database
-- adds table to the database hbtn_0c_0
CREATE TABLE IF NOT EXISTS SELECT hbtn_0c_0.first_table (
       id INT,
       name VARCHAR(256),
       PRIMARY KEY(id)
);
